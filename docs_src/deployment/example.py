"""Deployment-oriented CLI invocations."""

# Development with reload
# palfrey myapp.main:app --reload --reload-dir src

# Production with workers
# palfrey myapp.main:app --workers 4 --host 0.0.0.0 --port 8080
